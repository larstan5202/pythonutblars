def average_words(strings):
    found = []
    for item in strings:
        if 4 < len(item) < 8:
            founr.append(item)
        return found
average_words(["sup", "how's", "it", "going", "refelcting", "on", "programs", "and", "coding"])
